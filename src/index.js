import React from 'react';
import ReactDOM from 'react-dom';

import './index.css';
import App from './App';


//starting point. It is the first executed part of the whole code.

ReactDOM.render(<App />, document.getElementById('root'));
//const root = ReactDPM.createRoot(document.getElementById('root'));
//root.render(<App />);